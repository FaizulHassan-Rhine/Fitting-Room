# Single-project Vercel setup (root Next.js app serves /api/v1/* routes)

# Required
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority
NODE_ENV=production

# Optional distributed cache (if omitted, in-memory cache is used)
UPSTASH_REDIS_REST_URL=https://<your-redis-id>.upstash.io
UPSTASH_REDIS_REST_TOKEN=<your-token>

# Optional cache tuning
CACHE_TTL_SEARCH=300
CACHE_TTL_PRODUCT=600
CACHE_TTL_AUTOCOMPLETE=3600

# Optional API rate limiting (applies to /api/v1/* routes)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100
